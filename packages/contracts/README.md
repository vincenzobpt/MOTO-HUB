# Shared Contracts

This space will contain contracts without Android dependencies when a second
client (macOS, Linux or Windows) exists. Future examples include streaming
profiles, session events, redacted diagnostics and T-Box capabilities.

Do not move `MediaProjection`, `MediaCodec`, Wi-Fi, gomobile or UI code here:
they are Android-specific details.
