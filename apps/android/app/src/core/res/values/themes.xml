<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Fully transparent, draws no window content — used by RideDashboardTrampolineActivity,
         which exists only to briefly put Core in the TOP state and never shows anything. -->
    <style name="TrampolineTheme" parent="android:Theme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
